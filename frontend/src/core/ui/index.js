/**
 * core/ui — Public API barrel export
 *
 * Import shared UI components from here rather than their deep paths.
 *
 * Usage:
 *   import { Navbar } from '@/core/ui'
 */
export { default as Navbar } from './Navbar/Navbar'
